services:
  - type: web
    name: roku-realdebrid
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: REALDEBRID_API_KEY
        sync: false
      - key: PORT
        value: 3000
    healthCheckPath: /api/health